# Windows 打包说明

## ⚠ 重要提示

**由于 PyInstaller 打包的程序是系统相关的，必须在对应系统上打包：**

- **Windows exe 文件** → 必须在 **Windows 系统**上打包
- **macOS app 文件** → 必须在 **macOS 系统**上打包

**两者不能交叉使用！**

## 在 Windows 上打包步骤

### 1. 安装 Python（如果没有）
- 下载地址: https://www.python.org/downloads/
- 安装时勾选 "Add Python to PATH"

### 2. 安装依赖
```bash
pip install -r requirements.txt
pip install pyinstaller
```

### 3. 打包成 exe
```bash
python build_exe.py
```

### 4. 运行程序
打包完成后，在 `dist` 文件夹中找到 `PNG转JPG转换器.exe`，双击运行即可。

## 当前状态

✅ 已在 macOS 上测试：
- 程序功能正常
- PNG 转 JPG 转换成功
- 界面显示正常

❌ 需要在 Windows 上重新打包：
- 当前生成的是 macOS 版本
- 需要将代码复制到 Windows 电脑
- 在 Windows 上运行 `python build_exe.py`

## 快速方案

如果你现在就需要 Windows 版本，有两个选择：

1. **在 Windows 电脑上打包**（推荐）
   - 复制所有文件到 Windows
   - 运行 `pip install -r requirements.txt`
   - 运行 `python build_exe.py`

2. **直接运行 Python 脚本**（临时方案）
   - 在 Windows 上安装 Python 和依赖
   - 直接运行 `python png_to_jpg_converter.py`

## 文件清单

```
png_to_jpg_converter.py  # 主程序
requirements.txt         # 依赖列表
build_exe.py            # 打包脚本
README.md               # 使用说明
打包说明.md             # 本文件
```
